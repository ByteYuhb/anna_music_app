package com.anna.lib_image_loader.glide;

import android.widget.ImageView;



public class GlideAppManager {
    private void displayImageForImageView(ImageView imageView, String url){
//        GlideApp.with(imageView.getContext()).load(url).into(imageView);
    }
}
